---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { weddings } from '../../lib/weddings';
---

<BaseLayout
  title="Esküvői inspirációk — Mimóza Memories"
  description="Természetes, mezővirágos esküvői dekorációk, valódi párok történeteivel."
>
  <section class="section" aria-labelledby="wedding-list-heading">
    <header class="section-header">
      <h1 id="wedding-list-heading">Esküvők, amelyekben benne vagytok ti is</h1>
      <p class="section-intro">
        Minden dekoráció egyedi: a párok személyiségét, a helyszín karakterét és az évszak
        hangulatát egyszerre figyelembe véve születik meg.
      </p>
    </header>

    <div class="wedding-grid">
      {weddings.map((wedding) => (
        <article class="wedding-card">
          <a class="card-link-wrapper" href={`/wedding/${wedding.slug}`}>
            <div class="wedding-image-shell">
              <div
                class="wedding-image"
                style={`background-image: url('${new URL(
                  `../../../resources/wedding/${wedding.slug}/${wedding.slug}-profile.jpg`,
                  import.meta.url,
                ).href}');`}
              />
            </div>
            <div class="wedding-body">
              <h2>{wedding.name}</h2>
              <p>{wedding.caption}</p>
              <span class="card-cta">Megnézem az esküvőt</span>
            </div>
          </a>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>

<style>
  .section {
    padding: 2.4rem 2rem 2.6rem;
    border-radius: var(--radius-xl);
    background: rgba(255, 255, 255, 0.9);
    border: 1px solid rgba(224, 222, 217, 0.9);
    box-shadow: var(--shadow-soft);
  }

  .section-header {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    margin-bottom: 1.8rem;
  }

  .section-header h1 {
    margin: 0;
    font-size: 1.8rem;
  }

  .section-intro {
    margin: 0;
    color: var(--color-muted);
    max-width: 36rem;
  }

  .wedding-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.6rem;
  }

  .wedding-card {
    border-radius: 20px;
    overflow: hidden;
    background: #ffffff;
    border: 1px solid rgba(224, 222, 217, 0.9);
    min-height: 100%;
  }

  .card-link-wrapper {
    display: flex;
    flex-direction: column;
    height: 100%;
    color: inherit;
  }

  .wedding-image-shell {
    padding: 0.75rem 0.75rem 0;
  }

  .wedding-image {
    border-radius: 16px;
    height: 190px;
    background-size: cover;
    background-position: center;
  }

  .wedding-body {
    padding: 0.9rem 1.05rem 1.1rem;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .wedding-body h2 {
    margin: 0;
    font-size: 1.05rem;
  }

  .wedding-body p {
    margin: 0;
    color: var(--color-muted);
    font-size: 0.9rem;
  }

  .card-cta {
    margin-top: 0.6rem;
    font-size: 0.85rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-text);
  }

  @media (max-width: 900px) {
    .wedding-grid {
      grid-template-columns: minmax(0, 1fr);
    }
  }

  @media (max-width: 640px) {
    .section {
      padding-inline: 1.2rem;
    }
  }
</style>

